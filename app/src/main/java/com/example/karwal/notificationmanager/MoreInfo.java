package com.example.karwal.notificationmanager;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;

import static com.example.karwal.notificationmanager.MainActivity.isNotificationActive;
import static com.example.karwal.notificationmanager.MainActivity.mNotificationManager;
import static com.example.karwal.notificationmanager.MainActivity.notifID;

public class MoreInfo extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_more_info);

    }
}
